<script>

    let sdbundlePageType = '{{request.page_type}}';
    let sdbundleProductName = `{{ product.title }}`;
    let sdCurrencyIsoCode = '{{ shop.currency }}';
    let sdCurrencySymbolCode = '{{ shop.money_format }}';
    let sdCurrentDateTime = '{{ "now" | date: "%Y-%m-%dT%H:%M:%S.%3NZ"  }}'
    let sdProductJson = {{ product | json }};
    let sdBundleProductId = '';


    if (sdbundlePageType == "product") {
        sdBundleProductId = '{{product.id}}';
    }

    let sdBundleCollectionId = [];
    {% for collection in product.collections %}
    sdBundleCollectionId.push({{ collection.id }});
    {% endfor %}
</script>